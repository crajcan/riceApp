.mat-card
  = form_for(@reply) do |f|
    = render 'shared/error_messages', object: f.object
    = f.hidden_field :post_id, :value => post.id
    = f.text_area :content, placeholder: "Reply..."
    = f.submit "post reply", class: "form-submit"
    
