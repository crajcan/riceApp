%header
  =link_to "Pivot", root_path, :id => "logo"
  %ul
    %li= logged_in? ? (link_to "Log Out", logout_url, :method => :delete) : (link_to "Log in", login_url) 
    - if (!logged_in?)
      %li 
        = (link_to "Sing Up", signup_url)
    %li= link_to "Home",   home_url
