= form_for(current_user.active_relationships.find_by(unfollowed_id: @user.id), 
  html: { method: :delete }) do |f|#, remote: true) do |f|                                          
  = f.submit "follow", :class => 'relationship'

